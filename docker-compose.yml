services:

  backend:
    build: ./backend/BackendApi
    ports:
      - "5000:8080"
    volumes:
      - ./data:/app/data

  frontend:
    build: ./frontend/frontend-app
    ports:
      - "5173:5173"
